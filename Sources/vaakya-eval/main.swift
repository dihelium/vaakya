import VaakyaCore

// vaakya-eval — offline evaluation harness (task 2.6 builds this out).
// Will run recorded fixtures through ASR + personalization and report
// token error rate and latency. Placeholder for scaffold task 0.2.
print("vaakya-eval scaffold OK — VaakyaCore \(VaakyaCore.version)")
